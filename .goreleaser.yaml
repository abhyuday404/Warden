version: 2

project_name: prava-deploy

before:
  hooks:
    - go mod tidy
    - go test ./...

builds:
  - id: prava-deploy
    main: ./cmd/prava-deploy
    binary: prava-deploy
    env:
      - CGO_ENABLED=0
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w -X github.com/abhyuday404/prava-hack/internal/cli.Version={{.Version}}

archives:
  - formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: checksums.txt

changelog:
  use: git
